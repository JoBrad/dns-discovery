---
phase: 1
plan: 2
type: build
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - axeman/certlib.py
  - axeman/email_health.py
  - axeman/core.py
autonomous: true
requirements:
  - EMAIL-01 through EMAIL-05: MX/SPF/DMARC/DKIM validation
  - TLS-01 through TLS-04: TLS checks with error discrimination
  - OUT-01, OUT-02: Output structure and formatting
must_haves:
  - DKIM selector probing (27 selectors)
  - 4-pillar email health scoring
  - TLS validation with OpenSSL error code mapping
  - Final executive summary report in stdout
---

# Objective
Complete the CLI tool by integrating the remaining two pillars: TLS Health and Email Health, and finalize the output formatting to provide the requested executive summary.

# Context
- Reference: `.github/skills/spike-findings-dns-discovery/references/tls_health_checks.md`
- Reference: `.github/skills/spike-findings-dns-discovery/references/email_health_validation.md`
- Source: `.github/skills/spike-findings-dns-discovery/sources/tls_check_spike.py`
- Source: `.github/skills/spike-findings-dns-discovery/sources/email_health_spike.py`

# Tasks

<task id="1.2.1">
<title>TLS Health Pillar</title>
<read_first>.github/skills/spike-findings-dns-discovery/sources/tls_check_spike.py</read_first>
<files>
  - axeman/certlib.py
  - axeman/core.py
</files>
<action>
1. Refactor `axeman/certlib.py` to implement `TLSChecker.check_domain(domain)`.
2. Use `ssl.create_default_context()` and `sock.connect()`.
3. Map specific OpenSSL error codes: `[SSL: CERTIFICATE_VERIFY_FAILED] certificate has expired` -> Expired (10), `self-signed certificate` -> Self-signed (18/19), `hostname doesn't match` -> Mismatch (62).
4. Integrate into `axeman/core.py`.
</action>
<verify>
uv run dns-discovery github.com | grep "TLS"
</verify>
<done>
CLI output shows TLS status and specific error names if validation fails.
</done>
</task>

<task id="1.2.2">
<title>Email Health Pillar</title>
<read_first>.github/skills/spike-findings-dns-discovery/sources/email_health_spike.py</read_first>
<files>
  - axeman/email_health.py
  - axeman/core.py
</files>
<action>
1. Create `axeman/email_health.py`.
2. Implement checks for MX presence, SPF (parsable and no `+all`), DMARC (`p=reject` or `p=quarantine`), and DKIM.
3. For DKIM, use the 27-selector probe list from the spike.
4. Calculate a 4-pillar score (0-4).
5. Integrate into `axeman/core.py`.
</action>
<verify>
uv run dns-discovery cloudflare.com | grep "Email Health"
</verify>
<done>
CLI shows presence of MX, SPF, DMARC, and any discovered DKIM selectors.
</done>
</task>

<task id="1.2.3">
<title>Executive Summary Output</title>
<read_first>axeman/core.py</read_first>
<files>
  - axeman/core.py
</files>
<action>
1. Format the final output into a clean "Executive Summary".
2. Include sections for: Provider Discovery, DNS Summary, TLS Health, and Email Health Score.
3. Ensure the output is readable on stdout.
</action>
<verify>
uv run dns-discovery github.com
</verify>
<done>
A complete report is printed for github.com covering all 4 pillars.
</done>
</task>

# Verification
1. Run `dns-discovery github.com`. Verify 4-pillar score and TLS status.
2. Run `dns-discovery cloudflare.com`. Verify Mandrill DKIM selector discovery.

# Success Criteria
- CLI provides a complete "Discovery Report".
- TLS errors are correctly discriminated.
- DKIM probing works for known selectors.
- Overall Email Health Score is calculated.
