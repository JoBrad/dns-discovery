---
phase: 01-cli-tool
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - internal/discovery/tls.go
  - internal/discovery/email.go
  - cmd/dns-discovery/main.go
autonomous: true
requirements:
  - TLS-01
  - TLS-02
  - TLS-03
  - TLS-04
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
  - OUT-01
  - OUT-02
must_haves:
  truths:
    - User sees TLS health details and categorized TLS certificate failures.
    - User sees MX/SPF/DMARC/DKIM email DNS health and a 4-pillar score.
    - User sees a readable executive summary combining all four pillars.
  artifacts:
    - path: internal/discovery/tls.go
      provides: TLS handshake and certificate evaluation with categorized outcomes.
    - path: internal/discovery/email.go
      provides: MX/SPF/DMARC/DKIM checks and score computation.
    - path: cmd/dns-discovery/main.go
      provides: Final phase-1 stdout report assembly.
  key_links:
    - from: cmd/dns-discovery/main.go
      to: internal/discovery/tls.go
      via: per-host TLS checks for discovered web targets
      pattern: CheckTLS
    - from: cmd/dns-discovery/main.go
      to: internal/discovery/email.go
      via: domain email health evaluation call
      pattern: EvaluateEmailHealth
---

<objective>
Complete phase 1 by implementing TLS and email health pillars and presenting a consolidated, human-readable report on stdout.

Purpose: Deliver the full discovery behavior required for UAT domains and all remaining phase-1 requirements.
Output: TLS evaluator, email health evaluator, integrated executive summary output.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cli-tool/01-CONTEXT.md
@.planning/phases/01-cli-tool/01-01-PLAN.md
@.github/skills/spike-findings-dns-discovery/SKILL.md
@.github/skills/spike-findings-dns-discovery/references/tls-health.md
@.github/skills/spike-findings-dns-discovery/references/email-dns-health.md

<interfaces>
From internal/discovery/types.go (created in 01-01-PLAN.md):
- DiscoveryResult aggregates dns, providers, tls_checks, email_health, and summary fields.
- TLS result type includes status, tls_version, issuer, not_after, days_to_expiry, and error_category.
- Email health type includes mx_present, spf_present, dmarc_present, dkim_selectors, score, and findings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TLS health checks with categorized failure outcomes</name>
  <files>internal/discovery/tls.go, internal/discovery/types.go</files>
  <behavior>
    - Test 1: Valid TLS endpoint returns protocol version, issuer, and expiry metadata.
    - Test 2: Expired certificate maps to expired category (OpenSSL code 10 equivalent).
    - Test 3: Self-signed certificate maps to self-signed category (18/19 equivalent).
    - Test 4: Hostname mismatch maps to hostname-mismatch category (62 equivalent).
    - Test 5: Non-HTTPS target returns graceful unavailable result without aborting full run.
  </behavior>
  <action>Implement CheckTLS(hostname) using crypto/tls and x509 verification semantics to mirror spike error categories. Capture TLS version, issuer, expiry date, days-until-expiry, and warning when remaining validity is less than 14 days. Enforce 5-second timeout and non-fatal behavior for refused/timeout hosts per locked Error Handling decisions.</action>
  <verify>
    <automated>go test ./... && go run ./cmd/dns-discovery github.com</automated>
  </verify>
  <done>TLS pillar returns structured status for both healthy and failing TLS endpoints without stopping other checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement email DNS health with 27-selector DKIM probing and score</name>
  <files>internal/discovery/email.go, internal/discovery/types.go</files>
  <behavior>
    - Test 1: MX presence is detected with priority-preserving record capture.
    - Test 2: SPF record parsing identifies softfail, hardfail, and insecure +all.
    - Test 3: DMARC record parsing extracts policy and reporting status.
    - Test 4: DKIM probing checks the canonical 27 selectors and returns found selectors.
    - Test 5: Score is computed as MX/SPF/DMARC/DKIM passed pillars out of 4.
  </behavior>
  <action>Implement EvaluateEmailHealth(domain) with MX lookup, SPF extraction from TXT records, DMARC lookup at _dmarc.domain, and DKIM probes for the exact selector list locked in context. Return findings and score string in {score}/4 format, including notes for softfail SPF and missing DMARC reporting where applicable.</action>
  <verify>
    <automated>go test ./... && go run ./cmd/dns-discovery cloudflare.com</automated>
  </verify>
  <done>Email pillar reports all four checks and accurate score output for UAT domains.</done>
</task>

<task type="auto">
  <name>Task 3: Render executive summary on stdout using all four pillars</name>
  <files>cmd/dns-discovery/main.go</files>
  <action>Integrate DNS/provider outputs from plan 01 with new TLS and email outputs into a readable sectioned report. Include zone overview, provider discovery, configured services, TLS health summary, email health summary, and top executive findings. Keep output on stdout for phase 1; do not write report files yet (deferred to phase 2).</action>
  <verify>
    <automated>go run ./cmd/dns-discovery github.com && go run ./cmd/dns-discovery cloudflare.com</automated>
  </verify>
  <done>CLI prints a complete, human-readable discovery report covering all phase-1 pillars and requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovery engine to remote TLS endpoints | Untrusted remote certificates and handshake data enter parser/validator |
| Discovery engine to DNS TXT content | Untrusted TXT payloads can be malformed or oversized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | internal/discovery/tls.go | mitigate | Verify hostname against certificate SAN/CN and report mismatch category |
| T-01-08 | T | internal/discovery/email.go | mitigate | Parse SPF/DMARC using strict token checks and ignore invalid tokens safely |
| T-01-09 | D | internal/discovery/tls.go | mitigate | Use 5-second dial/handshake timeout and continue evaluation across targets |
| T-01-10 | I | cmd/dns-discovery/main.go | accept | Output only public DNS/TLS metadata; no sensitive local secrets in report |
| T-01-11 | R | cmd/dns-discovery/main.go | accept | Local CLI run does not require audit trail in phase 1 scope |
| T-01-12 | E | internal/discovery/email.go | accept | No privilege boundary; evaluator runs with caller permissions only |
</threat_model>

<verification>
Run:
1. go test ./...
2. go run ./cmd/dns-discovery github.com
3. go run ./cmd/dns-discovery cloudflare.com

Optional negative checks for TLS categorization:
4. go run ./cmd/dns-discovery expired.badssl.com
5. go run ./cmd/dns-discovery self-signed.badssl.com
6. go run ./cmd/dns-discovery wrong.host.badssl.com
</verification>

<success_criteria>
- TLS checks classify valid, expired, self-signed, mismatch, and unavailable endpoints.
- Email checks return MX/SPF/DMARC/DKIM findings with score out of 4.
- Executive summary includes all four pillars with readable sections.
- Phase 1 requirements TLS-01..04, EMAIL-01..05, OUT-01..02 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-tool/01-02-SUMMARY.md`
</output>
