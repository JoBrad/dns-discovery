---
phase: 1
plan: 1
type: build
wave: 1
depends_on: []
files_modified:
  - axeman/core.py
  - axeman/providers.py
  - pyproject.toml
autonomous: true
requirements:
  - CLI-01: CLI accepts domain argument
  - ENUM-01: Query all 9 DNS record types
  - PROV-01: Map NS to friendly provider names
  - PROV-02: Self-hosted detection
  - PROV-03: Split DNS detection
must_haves:
  - CLI "dns-discovery" script entry point
  - DNS enumeration for A, AAAA, MX, TXT, NS, CNAME, SOA, CAA, PTR
  - Provider fingerprinting logic using the ~60-entry table
---

# Objective
Establish the CLI entry point and implement the first two discovery pillars: DNS Enumeration and Provider Fingerprinting. This plan delivers a working CLI that identifies who hosts a domain's DNS and what records exist.

# Context
- Reference: `.github/skills/spike-findings-dns-discovery/references/dns_enumeration.md`
- Reference: `.github/skills/spike-findings-dns-discovery/references/provider_fingerprinting.md`
- Source: `.github/skills/spike-findings-dns-discovery/sources/dns_enum_spike.py`
- Source: `.github/skills/spike-findings-dns-discovery/sources/provider_fingerprint_spike.py`

# Tasks

<task id="1.1.1">
<title>CLI Entry Point and Project Config</title>
<read_first>pyproject.toml</read_first>
<files>
  - pyproject.toml
  - axeman/core.py
</files>
<action>
1. Update `pyproject.toml` to add `dns-discovery = "axeman.core:main"` to `[project.scripts]`.
2. Ensure `dnspython>=2.6.1` is in dependencies.
3. In `axeman/core.py`, implement a `main()` function using `argparse` that accepts a `domain` positional argument.
4. Implement a basic `DiscoveryEngine` class that will orchestrate the pillars.
</action>
<verify>
uv run dns-discovery github.com
</verify>
<done>
Command `dns-discovery` runs and prints the domain name.
</done>
</task>

<task id="1.1.2">
<title>DNS Enumeration Pillar</title>
<read_first>.github/skills/spike-findings-dns-discovery/sources/dns_enum_spike.py</read_first>
<files>
  - axeman/dns_scanner.py
  - axeman/core.py
</files>
<action>
1. Create `axeman/dns_scanner.py`.
2. Implement `DNSScanner.query_all(domain)` to fetch A, AAAA, MX, TXT, NS, CNAME, SOA, CAA.
3. Handle common `dnspython` exceptions (NXDOMAIN, NoAnswer, Timeout).
4. Integrate `DNSScanner` into `axeman/core.py`.
</action>
<verify>
uv run dns-discovery github.com | grep "Records found"
</verify>
<done>
CLI output shows counts or lists of DNS records for the domain.
</done>
</task>

<task id="1.1.3">
<title>Provider Fingerprinting Pillar</title>
<read_first>.github/skills/spike-findings-dns-discovery/references/provider_fingerprinting.md</read_first>
<files>
  - axeman/providers.py
  - axeman/core.py
</files>
<action>
1. Create `axeman/providers.py`.
2. Port the ~60 provider patterns from the spike findings (NS/CNAME/MX patterns).
3. Implement `ProviderDetector.identify(dns_data)`.
4. Implement logic for "Self-hosted" (NS matches domain) and "Split DNS" (Multiple providers in NS set).
5. Integrate into `axeman/core.py`.
</action>
<verify>
uv run dns-discovery github.com | grep "Provider:" && uv run dns-discovery cloudflare.com | grep "Provider:"
</verify>
<done>
CLI correctly identifies "GitHub" or "NS1/DigiCert" patterns and "Cloudflare" for the respective domains.
</done>
</task>

# Verification
1. Run `dns-discovery github.com`.
2. Confirm DNS records are listed.
3. Confirm provider is identified as Microsoft/NS1 variants or similar based on spike patterns.

# Success Criteria
- CLI tool is installable and runnable via `uv run dns-discovery`.
- DNS enumeration retrieves all 9 types (where present).
- Provider fingerprinting correctly labels GitHub and Cloudflare (or their underlying DNS infra).
