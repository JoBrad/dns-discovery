---
phase: 03-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - cmd/dns-discovery/main.go
  - internal/app/run.go
  - internal/app/run_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements:
  - BAT-01
  - BAT-02
  - ERR-01
must_haves:
  truths:
    - User can process multiple domains in one invocation from a JSON config file or an input file.
    - A failure for one domain is reported without preventing other domains from completing.
    - Batch runs end with a clear aggregate summary and a non-zero exit when any domain fails.
  artifacts:
    - path: internal/app/run.go
      provides: Shared single-domain and batch execution orchestration with result aggregation.
      exports: [RunDomain, RunBatch, BatchSummary]
    - path: internal/app/run_test.go
      provides: Automated coverage for partial-failure batch behavior and summary counts.
    - path: cmd/dns-discovery/main.go
      provides: CLI flags for batch input and exit-status handling.
  key_links:
    - from: cmd/dns-discovery/main.go
      to: internal/app/run.go
      via: command execution path chooses single-domain vs batch mode
      pattern: app.RunDomain|app.RunBatch
    - from: internal/app/run.go
      to: internal/config/config.go
      via: config-provided domains feed batch execution
      pattern: Config.Domains
---

<objective>
Add batch execution and resilient error reporting on top of the config contracts from Plan 03-01.

Purpose: Let the CLI process fleets of domains in one run without turning one bad domain into a full-run failure.
Output: Batch orchestration helpers, CLI wiring for input sources, and aggregate exit/reporting behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-integration/03-01-PLAN.md
@cmd/dns-discovery/main.go

<interfaces>
From Plan 03-01 / internal/config/config.go:
```go
type Config struct {
	OutputDir string   `json:"output_dir"`
	Domains   []string `json:"domains"`
}

func Load(path string) (Config, error)
func Resolve(flagOutputDir string, cfg Config) Config
```

Create in internal/app/run.go:
```go
type BatchSummary struct {
	Succeeded []string
	Failed    map[string]error
}

func RunDomain(domain string, outputDir string) error
func RunBatch(domains []string, outputDir string) BatchSummary
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract execution orchestration for single and batch runs</name>
  <files>internal/app/run.go, internal/app/run_test.go, cmd/dns-discovery/main.go</files>
  <behavior>
    - Test 1: A successful single-domain call still returns nil and produces the same report path behavior as today.
    - Test 2: A batch with one failing domain and one valid domain records one success and one failure.
    - Test 3: Batch summary exposes enough information for the CLI to print counts and domain-specific errors.
  </behavior>
  <action>Move the current domain execution path out of Cobra's `main.go` into a small orchestration package so the same logic can be reused for batch mode. Preserve the existing stdout report shape for single-domain runs. Introduce a `BatchSummary` contract that tracks succeeded and failed domains separately, and keep failures attached to the domain that caused them. Avoid concurrency in this phase; prioritize deterministic, readable output and straightforward error aggregation.</action>
  <verify>
    <automated>go test ./internal/app && go build ./...</automated>
  </verify>
  <done>The codebase has a reusable execution layer for one-domain and many-domain runs, with tests proving mixed-success batches are represented correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI batch inputs and aggregate exit behavior</name>
  <files>cmd/dns-discovery/main.go, internal/config/config.go, internal/config/config_test.go, internal/app/run.go</files>
  <behavior>
    - Test 1: `--input-file` accepts newline-delimited domains and processes each trimmed non-empty entry.
    - Test 2: Config `domains` from `.dns-discovery.json` runs as batch input when no positional domain is supplied.
    - Test 3: Any batch failure yields a non-zero command error after all domains are attempted.
  </behavior>
  <action>Add `--input-file` for newline-delimited domains and define input precedence clearly: positional domain first, then `--input-file`, then config `domains`. Reject invocations that provide no domains at all with a specific usage error. For batch runs, print a final summary with total/succeeded/failed counts and list failed domains with their root causes. Return a non-zero error if any domains fail, but only after every valid candidate has been attempted.</action>
  <verify>
    <automated>go test ./internal/app ./internal/config && go build ./...</automated>
  </verify>
  <done>The CLI supports multi-domain runs from input file or JSON config, continues past per-domain errors, and exits non-zero with a clear aggregate summary when any failures occur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input file -> process | Untrusted newline-delimited domain input controls network activity |
| config domains -> batch runner | Local JSON config can trigger repeated discovery attempts |
| batch summary -> exit code | Aggregated failures influence automation and CI behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | internal/app/run.go | mitigate | Trim and validate domain inputs before execution and attach errors to the offending domain only |
| T-03-05 | D | batch execution | mitigate | Process sequentially with bounded one-domain-at-a-time work so batch runs do not amplify resource usage unexpectedly |
| T-03-06 | R | CLI summary | mitigate | Emit deterministic success/failure counts and domain-specific error lines so automation can verify outcomes |
</threat_model>

<verification>
Run `go test ./internal/app ./internal/config` and `go build ./...`.
Smoke-check one happy-path batch and one mixed batch with a bad domain in `--input-file`.
</verification>

<success_criteria>
- Positional domain, input file, and JSON config domains have explicit precedence.
- Multi-domain runs continue after per-domain failures.
- Batch runs print a final aggregate summary and return non-zero when any domain fails.
- Single-domain behavior remains readable and compatible with the current workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration/03-02-SUMMARY.md`
</output>
