---
phase: 04-modular-output-and-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - cmd/dns-discovery/main.go
  - cmd/dns-discovery/main_test.go
  - internal/app/run.go
autonomous: true
requirements:
  - MOD-01
  - LOG-01
  - LOG-02
  - LOG-03
must_haves:
  truths:
    - User can choose output flavor using `output` with `markdown`, `json`, or `text`, and markdown remains the default.
    - User can control log location through config or CLI, with CLI taking precedence over config and default.
    - User can enable verbose logging while non-verbose runs keep error-level logging behavior.
  artifacts:
    - path: internal/config/config.go
      provides: Config contract fields and precedence helpers for output and log location.
      exports: [Config, Load, Resolve]
    - path: cmd/dns-discovery/main.go
      provides: CLI flags and runtime option wiring for output and logging.
    - path: internal/app/run.go
      provides: Output enum validation entrypoint used by orchestration.
  key_links:
    - from: cmd/dns-discovery/main.go
      to: internal/config/config.go
      via: runtime settings precedence
      pattern: Load|Resolve
    - from: cmd/dns-discovery/main.go
      to: internal/app/run.go
      via: validated output format passed into app options
      pattern: RunDiscovery|ValidateOutputFormat
---

<objective>
Establish the output and logging contracts for Phase 4 at the config/CLI boundary before orchestration refactors begin.

Purpose: Lock API-level behavior (output enum, log location precedence, verbose flag) so downstream orchestration and rendering work can proceed against stable contracts.
Output: Updated config schema, CLI flags, app-level output validation, and tests proving precedence/defaults.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modular-output-and-logging/04-CONTEXT.md
@cmd/dns-discovery/main.go
@internal/config/config.go
@internal/app/run.go

<interfaces>
From internal/config/config.go:
```go
type Config struct {
	OutputDir string   `json:"output_dir"`
	Domains   []string `json:"domains"`
}
```

Introduce in this plan:
```go
type OutputFormat string
const (
	OutputMarkdown OutputFormat = "markdown"
	OutputJSON     OutputFormat = "json"
	OutputText     OutputFormat = "text"
)
func ValidateOutputFormat(raw string) (OutputFormat, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config and CLI contracts for output and log location</name>
  <files>internal/config/config.go, internal/config/config_test.go, cmd/dns-discovery/main.go, cmd/dns-discovery/main_test.go</files>
  <behavior>
    - Test 1: Missing output selection defaults to markdown.
    - Test 2: `output` accepts markdown/json/text and rejects unsupported values with clear errors.
    - Test 3: log location resolves with precedence CLI > config > default logs path under repo root.
  </behavior>
  <action>Add `output` and `log_location` to the config contract, preserving backward compatibility for existing config files. Add CLI flags for `--output`, `--verbose`, and `--log-location`. Wire precedence so CLI values override config, and defaults apply when unset. Keep user-facing terminology `output` only (not `format`) per D-01 and D-02. Add tests covering defaulting, accepted enum values, and precedence behavior per D-08.</action>
  <verify>
    <automated>go test ./internal/config ./cmd/dns-discovery</automated>
  </verify>
  <done>CLI and config can express output flavor and log location with deterministic precedence and tested defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add app-layer output enum validation and options contract</name>
  <files>internal/app/run.go, internal/app/run_test.go</files>
  <behavior>
    - Test 1: app-level validation accepts markdown/json/text and normalizes case/whitespace policy.
    - Test 2: invalid output values fail fast before scan execution.
    - Test 3: options contract carries verbose and log location values into orchestration calls.
  </behavior>
  <action>Define the app-layer `OutputFormat` enum and validator in run-layer code so future non-CLI callers remain protected. Introduce an app options contract that includes output selection, verbose mode, and resolved log location. Keep this task contract-only: do not yet remove existing execution functions or rewrite report orchestration; that refactor is in Plan 04-02.</action>
  <verify>
    <automated>go test ./internal/app && go build ./...</automated>
  </verify>
  <done>App layer has validated output/logging options and rejects unsupported output values before orchestration proceeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config input -> app options | Untrusted user-supplied strings determine runtime output and log paths |
| log location -> filesystem | User-supplied paths may target invalid or unsafe locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | internal/config/config.go | mitigate | Validate output enum strictly and reject unknown values before orchestration |
| T-04-02 | E | cmd/dns-discovery/main.go | mitigate | Sanitize/normalize log path input and fail fast on invalid filesystem targets |
| T-04-03 | R | logging options | mitigate | Emit startup logs indicating resolved output and log-location sources (default/config/CLI) |
</threat_model>

<verification>
Run `go test ./internal/config ./cmd/dns-discovery ./internal/app` and `go build ./...`.
Smoke-check CLI behavior for:
- default output (markdown)
- explicit output (`json`, `text`)
- invalid output value
- log location override via config and CLI
</verification>

<success_criteria>
- Output enum and defaults are enforced at both CLI/config and app boundaries.
- Log location and verbose options are resolved deterministically with precedence rules.
- Existing invocation style remains backward compatible when new flags are omitted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modular-output-and-logging/04-01-SUMMARY.md`
</output>
